---
import { siteFonts } from "../../site-fonts.mjs";
import { Font } from "astro:assets";
---

{
  siteFonts.map((entry) => (
    <Font cssVariable={entry.cssVariable as "--font-body" | "--font-headings"} preload />
  ))
}
